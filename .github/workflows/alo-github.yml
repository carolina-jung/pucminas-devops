name: Exemplo de um fluxo de trabalho

on:
  workflow_dispatch:

jobs:
  acao_inicial_job:
    runs-on: ubuntu-latest
    steps:
      - name: Imprime ola
        run: echo "Olá"
      - name: Imprime tchau
        run: echo "tchau"
      
    
